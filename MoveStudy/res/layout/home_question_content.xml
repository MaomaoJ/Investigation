<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <include layout="@layout/above_title" />

    <ScrollView
        android:id="@+id/question_content_scrollView"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_marginTop="@dimen/title_height" >
    </ScrollView>

    <Button
        android:id="@+id/question_content_btn_next"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_margin="@dimen/brief_introduction_button_magin"
       android:background="@color/question_button"
       android:textColor="@color/white"
        android:text="@string/question_content_next" />

</FrameLayout>